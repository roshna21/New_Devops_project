PORT=5001
MONGODB_URI=mongodb://localhost:27017/nova-chat
JWT_SECRET=your_jwt_secret_here
NODE_ENV=development
SOLUTIONSYNC_URL=http://localhost:3000/api/analyze

# Exposes POST /crash, which kills the process so Kubernetes reports a pod
# restart. Demo only - never enable outside the self-healing demo.
ENABLE_CRASH_ENDPOINT=false
